# MACS+ Automation — Project Instructions

## Active App
We are only building `macs_automation/app.py` (the desktop UI) now — NOT the `macs_automation/web/` app.
All new routes, templates, and features go into `app.py` and `macs_automation/templates/`.
